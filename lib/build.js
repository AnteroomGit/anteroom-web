// Bumped by hand with each zip, matching the version number already used
// throughout tonight's build (anteroom-web__NN_.zip) -- not a real git
// hash, since this environment has no git history to draw one from, but
// serves the same purpose: every screenshot and every "fixed" claim can
// be tied to a specific, checkable build rather than an ambiguous "it
// should be fixed by now."
export const BUILD_STAMP = 'v63';
export const BUILD_TIME = '2026-09-25T10:52:00Z';
